# ระบบ UI/UX กลางของเว็บ (Tanot Design System)

เอกสารนี้บันทึกโทเคน/กติกากลางที่อยู่ใน `theme.css` (โหลดทุกหน้าคู่กับ `shell.js`)
เพื่อให้แก้ไข/เพิ่มหน้าใหม่ในอนาคตอ้างอิงชุดเดียวกัน แทนการ hardcode ตัวเลขใหม่ทุกครั้ง

## โทเคนสี (มีอยู่แล้วตั้งแต่รอบรวมศูนย์สี)
`--ome-bg` `--ome-card` `--ome-ink` `--ome-muted` `--ome-line` `--ome-brand`
`--ome-brand-dk` `--ome-brand-sf` `--ome-violet` `--ome-amber` `--ome-rose`
`--ome-sky` `--ome-ok` `--ome-warn` `--ome-err` — สลับค่าอัตโนมัติเมื่อ `data-theme="dark"`

## มาตราระยะห่าง (spacing scale)
หน่วยฐาน 4px ใช้แทนเลข px ลอยๆ เวลาตั้ง `margin`/`gap`/`padding` ใหม่:

| ตัวแปร | ค่า | ใช้กับ |
|---|---|---|
| `--ome-sp-1` | 4px | ระยะห่างจิ๋ว (ไอคอนกับข้อความ) |
| `--ome-sp-2` | 8px | ระยะห่างปุ่มในแถวเดียวกัน |
| `--ome-sp-3` | 12px | ระยะห่างฟิลด์ฟอร์ม |
| `--ome-sp-4` | 16px | ระยะห่างมาตรฐานทั่วไป |
| `--ome-sp-5` | 24px | ระยะห่างระหว่าง section ย่อย |
| `--ome-sp-6` | 32px | ระยะห่างระหว่าง section ใหญ่ |
| `--ome-sp-7` | 48px | ระยะห่างหัว/ท้ายหน้า |

## มาตราขนาดตัวอักษร (type scale)
อิงจากขนาดที่ใช้จริงมากที่สุดในเว็บนี้อยู่แล้ว (ไม่ใช่เลขคิดใหม่):

| ตัวแปร | ค่า | ใช้กับ |
|---|---|---|
| `--ome-fs-xs` | 12.5px | ป้ายกำกับเล็ก, `.mini` |
| `--ome-fs-sm` | 13.5px | ข้อความรอง |
| `--ome-fs-base` | 15px | เนื้อหาหลัก (body) |
| `--ome-fs-md` | 16px | ปุ่ม/ฟิลด์กรอก |
| `--ome-fs-lg` | 18px | หัวข้อย่อย |
| `--ome-fs-xl` | 21px | h1 มาตรฐาน |
| `--ome-fs-2xl` | 24px | หัวข้อใหญ่พิเศษ |
| `--ome-fs-3xl` | 28px | หัวหน้า landing |

**หมายเหตุ**: ตอนนี้เป็นชุดอ้างอิงสำหรับหน้าที่แก้ใหม่/สร้างใหม่ — หน้าเดิม 43 หน้ายังไม่ได้
ไล่แก้ `font-size` ทุกจุดให้ตรงตัวแปรนี้ทั้งหมด (เสี่ยงต่อ layout สูงกว่าการรวมศูนย์สี
เพราะขนาดตัวอักษรกระทบการตัดบรรทัด/ความสูงองค์ประกอบ) ทำเฉพาะจุดที่ค่าตรงกันอยู่แล้ว
(ดูหัวข้อ "การ์ด" ด้านล่าง) ส่วนที่เหลือค่อยทยอยแก้เวลาแตะไฟล์นั้นอยู่แล้ว

## การ์ด (Card component)
`--ome-card-r:16px` `--ome-card-pad:18px` `--ome-card-gap:14px`

19 หน้าใช้ค่าที่ตรงกันอยู่แล้ว (`border-radius:16px;padding:18px;margin-top:14px`)
เลยรวมเป็นตัวแปรกลางให้ (ไม่เปลี่ยนภาพที่เห็นเลยสักพิกเซล) — หน้าใหม่ควรเขียน:
```css
.card{background:var(--card);border-radius:var(--ome-card-r);
  box-shadow:var(--sh);padding:var(--ome-card-pad);margin-top:var(--ome-card-gap)}
```
`cad.html` override `--ome-card-pad:14px` ในหน้าตัวเอง (การ์ดแน่นกว่าเพราะมีแถบเครื่องมือเยอะ)
— รูปแบบ override นี้ใช้ได้กับหน้าอื่นที่มีเหตุผลเฉพาะตัวเหมือนกัน

Pages ที่ **ยังไม่ได้แตะ** (มี `.card` แบบอื่น หรือไม่มี `.card` เลย) เพราะค่าต่างจาก
มาตรฐานจริง ไม่ใช่แค่ยังไม่ได้อ้างตัวแปร: `documents.html`/`run.html` (padding 26px,
ใช้ `--r` คนละตัวแปร), หน้ากลุ่ม "เรียนทักษะ" (sports/music/cooking/typing/coding ฯลฯ)
ที่ยังไม่มี `.card` เลย, และ `invest.html`/`excel.html` ที่ h1 ใช้ font-size ต่างออกไปโดยตั้งใจ

## Accessibility & Micro-interactions (ใช้ได้ทันทีทุกหน้า ไม่ต้องแก้อะไรเพิ่ม)

- **Focus ring**: ทุก element ที่ focus ด้วยคีย์บอร์ด (Tab) จะมีกรอบเขียวมิ้นต์เดียวกันหมด
  (`:focus-visible`) ไม่โผล่ตอนคลิกด้วยเมาส์
- **ปุ่มกดมีเฟดแบ็ก**: `button`/`.btn`/`a.btn` ทุกปุ่มยุบเบาๆ (`scale(.97)`) ตอนกดค้าง
  ถ้าเบราว์เซอร์ตั้ง "ลดการเคลื่อนไหว" (`prefers-reduced-motion: reduce`) ไว้ อนิเมชันทั้งหมด
  ในเว็บ (รวมของเดิมที่มีอยู่ก่อน) จะถูกตัดให้เกือบเป็นศูนย์อัตโนมัติ

## ยูทิลิตี้ใหม่ที่เรียกใช้ได้ (ยังไม่ได้ต่อสายเข้าเครื่องมือไหนโดยอัตโนมัติ —รอ Group B)

- **`.ome-skeleton`** — กรอบเทาสั่นไหวแทนตัวเลข/ข้อความที่กำลังรอโหลดจากเน็ต
  ```html
  <span class="ome-skeleton" style="width:60px"></span>
  ```
- **`.ome-flash-ok`** — ใส่ชั่วคราวหลังบันทึกสำเร็จ พื้นหลังกระพริบเขียวมิ้นต์แล้วจางหาย
  ```js
  el.classList.add('ome-flash-ok');
  setTimeout(() => el.classList.remove('ome-flash-ok'), 900);
  ```
