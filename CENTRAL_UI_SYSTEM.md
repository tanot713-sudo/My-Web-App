# ระบบ UI/UX กลางของเว็บ (Tanot Design System)

เอกสารนี้บันทึกโทเคน/กติกากลางที่อยู่ใน `theme.css` (โหลดทุกหน้าคู่กับ `shell.js`)
เพื่อให้แก้ไข/เพิ่มหน้าใหม่ในอนาคตอ้างอิงชุดเดียวกัน แทนการ hardcode ตัวเลขใหม่ทุกครั้ง

## โทเคนสี (มีอยู่แล้วตั้งแต่รอบรวมศูนย์สี)
`--ome-bg` `--ome-card` `--ome-ink` `--ome-muted` `--ome-line` `--ome-brand`
`--ome-brand-dk` `--ome-brand-sf` `--ome-violet` `--ome-amber` `--ome-rose`
`--ome-sky` `--ome-ok` `--ome-warn` `--ome-err` — สลับค่าอัตโนมัติเมื่อ `data-theme="dark"`

## มาตราระยะห่าง (spacing scale)
หน่วยฐาน 4px ใช้แทนเลข px ลอยๆ เวลาตั้ง `margin`/`gap`/`padding` ใหม่:

| ตัวแปร | ค่า | ใช้กับ |
|---|---|---|
| `--ome-sp-1` | 4px | ระยะห่างจิ๋ว (ไอคอนกับข้อความ) |
| `--ome-sp-2` | 8px | ระยะห่างปุ่มในแถวเดียวกัน |
| `--ome-sp-3` | 12px | ระยะห่างฟิลด์ฟอร์ม |
| `--ome-sp-4` | 16px | ระยะห่างมาตรฐานทั่วไป |
| `--ome-sp-5` | 24px | ระยะห่างระหว่าง section ย่อย |
| `--ome-sp-6` | 32px | ระยะห่างระหว่าง section ใหญ่ |
| `--ome-sp-7` | 48px | ระยะห่างหัว/ท้ายหน้า |

## มาตราขนาดตัวอักษร (type scale)
อิงจากขนาดที่ใช้จริงมากที่สุดในเว็บนี้อยู่แล้ว (ไม่ใช่เลขคิดใหม่):

| ตัวแปร | ค่า | ใช้กับ |
|---|---|---|
| `--ome-fs-xs` | 12.5px | ป้ายกำกับเล็ก, `.mini` |
| `--ome-fs-sm` | 13.5px | ข้อความรอง |
| `--ome-fs-base` | 15px | เนื้อหาหลัก (body) |
| `--ome-fs-md` | 16px | ปุ่ม/ฟิลด์กรอก |
| `--ome-fs-lg` | 18px | หัวข้อย่อย |
| `--ome-fs-xl` | 21px | h1 มาตรฐาน |
| `--ome-fs-2xl` | 24px | หัวข้อใหญ่พิเศษ |
| `--ome-fs-3xl` | 28px | หัวหน้า landing |

**หมายเหตุ**: ตอนนี้เป็นชุดอ้างอิงสำหรับหน้าที่แก้ใหม่/สร้างใหม่ — หน้าเดิม 43 หน้ายังไม่ได้
ไล่แก้ `font-size` ทุกจุดให้ตรงตัวแปรนี้ทั้งหมด (เสี่ยงต่อ layout สูงกว่าการรวมศูนย์สี
เพราะขนาดตัวอักษรกระทบการตัดบรรทัด/ความสูงองค์ประกอบ) ทำเฉพาะจุดที่ค่าตรงกันอยู่แล้ว
(ดูหัวข้อ "การ์ด" ด้านล่าง) ส่วนที่เหลือค่อยทยอยแก้เวลาแตะไฟล์นั้นอยู่แล้ว

## การ์ด (Card component)
`--ome-card-r:16px` `--ome-card-pad:18px` `--ome-card-gap:14px`

19 หน้าใช้ค่าที่ตรงกันอยู่แล้ว (`border-radius:16px;padding:18px;margin-top:14px`)
เลยรวมเป็นตัวแปรกลางให้ (ไม่เปลี่ยนภาพที่เห็นเลยสักพิกเซล) — หน้าใหม่ควรเขียน:
```css
.card{background:var(--card);border-radius:var(--ome-card-r);
  box-shadow:var(--sh);padding:var(--ome-card-pad);margin-top:var(--ome-card-gap)}
```
`cad.html` override `--ome-card-pad:14px` ในหน้าตัวเอง (การ์ดแน่นกว่าเพราะมีแถบเครื่องมือเยอะ)
— รูปแบบ override นี้ใช้ได้กับหน้าอื่นที่มีเหตุผลเฉพาะตัวเหมือนกัน

Pages ที่ **ยังไม่ได้แตะ** (มี `.card` แบบอื่น หรือไม่มี `.card` เลย) เพราะค่าต่างจาก
มาตรฐานจริง ไม่ใช่แค่ยังไม่ได้อ้างตัวแปร: `documents.html`/`run.html` (padding 26px,
ใช้ `--r` คนละตัวแปร), หน้ากลุ่ม "เรียนทักษะ" (sports/music/cooking/typing/coding ฯลฯ)
ที่ยังไม่มี `.card` เลย, และ `invest.html`/`excel.html` ที่ h1 ใช้ font-size ต่างออกไปโดยตั้งใจ

## Accessibility & Micro-interactions (ใช้ได้ทันทีทุกหน้า ไม่ต้องแก้อะไรเพิ่ม)

- **Focus ring**: ทุก element ที่ focus ด้วยคีย์บอร์ด (Tab) จะมีกรอบเขียวมิ้นต์เดียวกันหมด
  (`:focus-visible`) ไม่โผล่ตอนคลิกด้วยเมาส์
- **ปุ่มกดมีเฟดแบ็ก**: `button`/`.btn`/`a.btn` ทุกปุ่มยุบเบาๆ (`scale(.97)`) ตอนกดค้าง
  ถ้าเบราว์เซอร์ตั้ง "ลดการเคลื่อนไหว" (`prefers-reduced-motion: reduce`) ไว้ อนิเมชันทั้งหมด
  ในเว็บ (รวมของเดิมที่มีอยู่ก่อน) จะถูกตัดให้เกือบเป็นศูนย์อัตโนมัติ

## ยูทิลิตี้ใหม่ที่เรียกใช้ได้ (ยังไม่ได้ต่อสายเข้าเครื่องมือไหนโดยอัตโนมัติ —รอ Group B)

- **`.ome-skeleton`** — กรอบเทาสั่นไหวแทนตัวเลข/ข้อความที่กำลังรอโหลดจากเน็ต
  ```html
  <span class="ome-skeleton" style="width:60px"></span>
  ```
- **`.ome-flash-ok`** — ใส่ชั่วคราวหลังบันทึกสำเร็จ พื้นหลังกระพริบเขียวมิ้นต์แล้วจางหาย
  ```js
  el.classList.add('ome-flash-ok');
  setTimeout(() => el.classList.remove('ome-flash-ok'), 900);
  ```

## สีธีมเลือกได้หลายแบบ (เฟส 4-4.1 ถอดจากภาพ UI/UX ที่ผู้ใช้ส่งมา, เฟส 6 แยกแกนสี/ความสว่าง)

**สถาปัตยกรรมปัจจุบัน (เฟส 6) — 2 แกนอิสระ**:
1. **`data-accent`** — ตัวตนสี (9 แบบ ดูตารางล่าง) กำหนดผ่านเมนูตั้งค่า → "เลือกธีมเว็บ"
2. **`data-theme`** — สว่าง/มืดล้วนๆ (ปุ่ม ☀️/🌙 มุมขวาบนเดิม) ใช้ได้กับทุกสีอิสระจากแกนที่ 1

ทั้งสองแกนรวมกันบน `<html>` พร้อมกัน (`data-accent="crypto" data-theme="dark"` = ฟ้าคริปโต
เวอร์ชันมืด) — เปลี่ยนสีจะไม่กระทบความสว่างที่ตั้งไว้ และกลับกัน บันทึกแยกคีย์กัน:
`localStorage['ome:accent']` (9 ค่า) กับ `localStorage['ome:theme']` (แค่ `light`/`dark`)

*เดิม (เฟส 4-4.1) ผูกสี+ความสว่างไว้ค่าเดียว (`ome:theme` เก็บทั้ง `'flooks'`/`'dark'` ปนกัน)
ทำให้เลือกสีอื่นแล้วติดสว่างเสมอ ไม่มีเวอร์ชันมืด — มีสคริปต์ย้ายค่าเก่าอัตโนมัติครั้งเดียวใน
`shell.js` (`migrateOldThemeKey()`): ค่าที่ไม่ใช่ `light`/`dark` จะถูกย้ายไปเป็น `ome:accent`
แล้วรีเซ็ต `ome:theme` เป็น `light` (ของเดิมเป็นเวอร์ชันสว่างล้วน ไม่เพี้ยนจากที่เคยเห็น)*

| id (`data-accent`) | ชื่อที่แสดง | สีหลัก | แรงบันดาลใจ |
|---|---|---|---|
| `mint` | เขียวมิ้นต์ (ปกติ) | เขียวมิ้นต์ `#12A594` | ธีมเดิมของเว็บ |
| `flooks` | เหลืองพลัง | เหลืองทอง `#FFC700` | แดชบอร์ดกีฬา/เดิมพันโทนเหลือง-ครีม |
| `gymes` | แดงสปอร์ต | แดง `#E5384D` | แดชบอร์ดฟิตเนสโทนแดง-ขาวคมชัด |
| `skypastel` | ฟ้าพาสเทล | ฟ้า `#3D7CF4` | แอปสไตล์กระจกโทนพาสเทลฟ้า-ม่วง (id เดิมชื่อ `glass` เปลี่ยนแล้ว กันชนกับ "ประเภทธีม: กระจกฝ้า" ในอนาคต) |
| `coach` | มินต์-ลาเวนเดอร์ | เขียวมินต์เข้ม `#1C9C88` | แดชบอร์ดกีฬาโทนมินต์พาสเทล |
| `finset` | ม่วงการเงิน | ม่วง `#7C6FEA` | แดชบอร์ดการเงินโทนม่วงอ่อน |
| `bubblegum` | ชมพูสดใส | ชมพูบานเย็น `#EC1E79` | แดชบอร์ดสไตล์กราฟฟิก 3 มิติโทนชมพูสดใส |
| `construct` | ส้มก่อสร้าง | ส้ม `#E8743B` | แดชบอร์ดบริหารโครงการก่อสร้างโทนครีม-ส้ม |
| `crypto` | ฟ้าคริปโต | ฟ้า `#2F7BFF` | แอปกระเป๋าเงินคริปโตโทนฟ้าสด |

ทุกสี (ยกเว้น mint ที่มีอยู่แล้ว) มีเวอร์ชันมืดของตัวเองครบแล้ว (`[data-accent="X"]
[data-theme="dark"]{...}`) — ถอดจากสูตรเดียวกับ mint dark เดิม (bg เข้มมาก, card เข้มรองลง
มา, ink เกือบขาว, brand คงสีเดิมหรือสว่างขึ้นเล็กน้อยให้เด่นบนพื้นมืด, brand-dk สว่างกว่า brand
ปกติ (สลับตรรกะจากโหมดสว่างเพราะใช้เป็นสีลิงก์บนพื้นมืด), brand-sf เป็นโทนเข้มของสีนั้นแทนโทนอ่อน)

**สถาปัตยกรรมโทเคน**: แต่ละสีกำหนดแค่โทเคนพื้นผิวหลัก 8 ตัว (`--ome-bg/card/ink/muted/line/
brand/brand-dk/brand-sf` + `--ome-sh`) ใน `theme.css` — **ไม่แตะ** สีความหมาย (`--ome-ok/err/
warn/violet/amber/rose/sky` ที่แต่ละหน้า hardcode เอง) เพื่อไม่ให้ความหมายสี (เขียว=กำไร/ผ่าน,
แดง=ขาดทุน/ผิด) เพี้ยนไปตามธีมที่เลือก — สอดคล้องกับ bento tile ในหน้าแรกที่แถบสีซ้ายจัดกลุ่ม
ตามหมวดงานยังคงสีเดิมทุกธีม

พื้นหลังหน้าเป็น**สีทึบล้วน** ไม่ใช้ gradient จากภาพต้นฉบับตรงๆ (แม้บางภาพอ้างอิงมี gradient
พาสเทลจริง) เพราะ `--ome-bg` ถูกใช้เป็นสี fill เรียบๆ ของ element เล็กๆ หลายร้อยจุดทั่วเว็บ
(hover, ช่อง input, แถบ progress, chip ฯลฯ) — ใส่ gradient จะเพี้ยนตรงจุดเล็กๆ พวกนั้น ใช้สี
โทนเฉลี่ยของ gradient แทน แล้วให้สีแบรนด์ + สไตล์การ์ด/มุมโค้งเป็นตัวถ่ายทอดบุคลิกของดีไซน์
ต้นแบบแทน

กลไกครอบคลุมหน้าที่มี `body.ome-tool-page` (35/43 ไฟล์) โดยอัตโนมัติ ไม่ต้องแก้ทีละหน้า — ผ่าน
rule เดียวใน `theme.css`: `[data-theme="dark"] body.ome-tool-page, [data-accent] body.ome-tool-page
{--bg:var(--ome-bg);...}` (เฟส 6 ทำให้ง่ายลง — เดิมต้องแจกแจงชื่อสีทีละอัน เพราะสี+ความสว่างเคย
ผูกกันในตัวแปรเดียว ตอนนี้แค่เช็คว่า "มี data-accent อยู่ไหม" ก็พอ ไม่ต้องรู้ว่าเป็นสีอะไร)

`.hero` (การ์ดต้อนรับ) ของสีที่ไม่ใช่ mint ใช้สูตรผสม `--ome-brand-sf` เดียวกันทั้งสว่าง/มืด
(`[data-accent]:not([data-accent="mint"]) body.ome-tool-page .hero{...}`) — mint เองยังคง
gradient moody เดิมตอนกดมืด (ของเดิมก่อนมีเฟส 4 เลย ไม่แตะ) เพราะ specificity ของ rule สี
ชนะเฉพาะตอนเลือกสีอื่นที่ไม่ใช่ mint เท่านั้น

**เฟส 6.1 — แก้หน้า React+Tailwind ให้ตามสีธีมด้วย** (`ome-app-page`: classroom-business/
engineering, languages, legal) ผู้ใช้แจ้งว่าหน้าพวกนี้ยังไม่เปลี่ยนสีตามธีมที่เลือกเลย —
เดิม override utility class ของ Tailwind (`bg-white`/`text-slate-800`/ฯลฯ) เป็นเลขสี hardcode
ตายตัวและมีแค่เงื่อนไข `[data-theme="dark"]` (ผูกกับ mint dark ตรงๆ) แก้โดยเปลี่ยนเลข hardcode
เป็น `var(--ome-*)` ทั้งหมด (เปลี่ยนตามสี+ความสว่างที่เลือกอัตโนมัติอยู่แล้ว) แล้วขยาย selector
เป็น `[data-accent]` ด้วย (ครอบคลุมทุกสีทั้งสว่าง/มืด ไม่ใช่แค่ dark) — เฉพาะ utility ที่เป็น
"พื้นผิวกลาง/แบรนด์" เท่านั้น (bg-white, bg-slate-50/gray-50, bg-slate-100/gray-100,
bg-brandLight, border-brand, text-slate-*/gray-*, border-slate-*/gray-*, textarea/input)
ส่วน utility ที่เป็น "สีความหมาย" (amber-50/emerald-50/green-50/red-50/blue-50 ใช้เป็นกล่อง
เตือน/สำเร็จ/ผิดพลาด/ข้อมูล) ยังคง hardcode เฉพาะ dark เหมือนเดิม ไม่ผูกกับสีธีม (นโยบายเดียว
กับที่เหลือทั้งเว็บ) — จุดที่ไม่มีตัวแปรตรงตัว (bg-slate-100/gray-100 ที่เดิมเป็นเฉดที่ 3 ระหว่าง
card กับ bg, text-slate-700/600 ที่เดิมเป็นเฉดกลางระหว่าง ink กับ muted) ใช้ `color-mix(in srgb,
...)` ผสมจาก 2 โทเคนแทน เพื่อให้ได้เฉดกลางที่ปรับตามสีธีมอัตโนมัติโดยไม่ต้องคิดค่าใหม่ทีละสี

**ยังทดสอบด้วยตาไม่ได้ในรอบนี้ (ตอนเฟส 6.1)**: 3 ใน 4 หน้านี้โหลด React/Babel จาก unpkg.com
(CDN ภายนอก) ซึ่ง sandbox ทดสอบของ Claude บล็อกโดเมนนี้ไว้ ทำให้เห็นแค่พื้นหลังหน้า (ยืนยัน
ถูกต้องแล้ว — เปลี่ยนสีตามธีมจริง) แต่มองไม่เห็นสีการ์ด/ตัวอักษรจริงที่ React render ออกมา —
ผู้ใช้เลยช่วยแคปหน้าจอจากเว็บจริงมาตรวจ พบจุดที่ยังไม่ถูก แก้ต่อในเฟส 6.2 ด้านล่าง

**เฟส 6.2 — แก้ custom color "primary/secondary/brand" ที่ผูก hex ตายตัวใน tailwind.config
ของแต่ละหน้า** ผู้ใช้ส่งภาพหน้าจอจากเว็บจริงมาตรวจหลังเฟส 6.1 พบว่าปุ่ม/ไอคอน/แถบไล่สีต้อนรับ
ในหน้า classroom-business/engineering ยังเป็นสีเขียวมิ้นต์ตลอดไม่ว่าจะเลือกสีธีมไหน — สาเหตุจริง
ไม่ใช่ปัญหาจากเฟส 6.1 (utility ของ Tailwind ธรรมดาที่แก้ไปแล้ว) แต่เป็น custom color ที่แต่ละ
หน้าประกาศเองใน `tailwind.config` (`primary:'#0B7F72'`, `secondary:'#12A594'` ในหน้า classroom-*
กับ legal, `brand:'#12A594'`, `brandLight:'#E7F6F4'` ในหน้า languages) ซึ่งบังเอิญ hex ตรงกับ
mint เป๊ะ (เพราะตอนออกแบบมีแค่ธีมมิ้นต์) เลยดูเหมือนใช้ได้ตอนธีมมิ้นต์ แต่ hardcode ไว้ตายตัว
ไม่ขยับตามเลือกสีอื่นเลย ใช้กับ utility หลายแบบ (`bg-/text-/border-primary`, `bg-/text-/
border-secondary`, `hover:`/`focus:` ของ secondary, `from-primary`/`to-secondary` ใน gradient
ต้อนรับ, `bg-/text-/border-brand`, `bg-brandLight`, `bg-brand/10`, `border-brand/20`)

แก้โดยเพิ่ม override ใน `theme.css` แมป utility เหล่านี้กลับไปที่คู่โทเคนเดิมของมันตรงๆ
(`primary`→`--ome-brand-dk`, `secondary`→`--ome-brand`, `brand`→`--ome-brand`, `brandLight`→
`--ome-brand-sf` — ตรงกับ hex เดิมเป๊ะที่ธีมมิ้นต์ ไม่มีอะไรเปลี่ยนตอนเลือกมิ้นต์) ด้วยแพทเทิร์น
เดียวกับเฟส 6.1 (`[data-theme="dark"] .ome-app-page .bg-primary, [data-accent] .ome-app-page
.bg-primary{...}`) ส่วน gradient stop (`--tw-gradient-from`/`--tw-gradient-to`) override ตรงตัว
แปรที่ Tailwind ใช้ประกอบ gradient เอง ไม่ต้องยุ่งกับ `--tw-gradient-stops` ที่ class ต้นทางตั้งไว้
แล้ว และตัดสี `via-teal-800` (hardcode เขียวเข้มไว้กลาง gradient ต้อนรับ ไม่ผูกกับ token ไหนเลย)
ออกจาก className ตรงๆ ในทั้ง 2 ไฟล์ ให้เหลือ gradient 2 สี `from-primary to-secondary` ที่ตาม
ธีมได้ครบ

ทดสอบด้วยหน้าจำลอง (ประกาศ `.bg-primary`/`.bg-secondary`/ฯลฯ เองแบบเดียวกับที่ Tailwind CDN
จะ generate ให้ เพราะ sandbox โหลด CDN จริงไม่ได้) ยืนยันด้วย `getComputedStyle` ว่าทุก utility
ที่แก้ไล่สีตาม accent+ความสว่างที่ตั้งค่าไว้ถูกต้องครบ (ทดสอบ mint/gymes/crypto ทั้งสว่าง/มืด)
รวมถึง gradient stops และตัวที่มี opacity modifier (`/10`, `/20`) — ยังต้องรอผู้ใช้ช่วยดูบนเว็บ
จริงอีกรอบเช่นเดิม เพราะข้อจำกัด CDN บล็อกใน sandbox ยังเหมือนเดิม

**เฟส 6.3 — หน้า languages.html หลุดจุดเดียวที่เฟส 6.2 มองข้าม**: ผู้ใช้ทดสอบเว็บจริงหลังเฟส 6.2
แล้วส่งภาพมาอีกรอบ พบว่า "เหลืองพลัง" เลือกแล้วแถบต้อนรับของหน้า languages.html ยังไล่จากเหลือง
(ซ้าย) ไปเขียว/ฟ้าอมเขียว (ขวา) อยู่ — ต่างจาก classroom-*.html ตรงที่หน้านี้ gradient ต้อนรับใช้
`from-brand via-teal-700 to-cyan-800` (มี 3 จุดสี ไม่ใช่ 2 แบบ classroom-*) เฟส 6.1/6.2 แก้แค่
`from-brand` (จุดเดียว) เพราะตอนนั้นยังไม่เจอว่ามี `via`/`to` hardcode ต่อท้ายอยู่ด้วย

หน้านี้ต่างจาก 3 หน้าที่เหลือตรงที่ logic ของ React component อยู่ใน `languages.jsx` (ซอร์สจริง)
แล้ว compile เป็น `languages.compiled.js` ด้วย esbuild ผ่าน `./build-languages.sh` (ไม่มี Babel-
in-browser แล้ว) เลยแก้ที่ `languages.jsx` โดยตรง (บรรทัดของ `bg-gradient-to-br` ในส่วน
LanguagesStreamView) แล้ว build ใหม่ ไม่ใช่ไปแก้ใน `.compiled.js` ตรงๆ

แก้โดยตัด `via-teal-700 to-cyan-800` (hardcode ไม่ผูกกับ token ไหนเลย ต่างจาก `primary`/
`secondary` ของ classroom-* ที่อย่างน้อยยังเป็น custom color ที่ override ได้) ออก แทนด้วย
Tailwind arbitrary-value syntax ตรงๆ: `to-[var(--ome-brand-dk)]` — อ้างตัวแปร CSS ของเว็บโดยตรง
ไม่ต้องเพิ่ม override ใน theme.css อีกชั้น (ยืนยันแล้วว่า Tailwind CDN เวอร์ชันนี้รองรับ arbitrary
value อยู่แล้ว เพราะบันเดิลเดิมมีการใช้ `h-[15px]` แบบเดียวกันอยู่ก่อน) ผลคือ gradient ต้อนรับ
ไล่จาก `--ome-brand` (สดใส) ไป `--ome-brand-dk` (เข้ม) ของสีที่เลือกเสมอ

ทดสอบ: `./build-languages.sh` ผ่าน (มี `node --check` ในตัวสคริปต์อยู่แล้ว), grep ยืนยันไม่เหลือ
`via-/to-` แบบ hardcode สี teal/cyan/emerald/green ใน compiled bundle อีกแล้ว, จำลอง gradient
utility ที่ Tailwind จะ generate ด้วยหน้าเทสต์แยก ยืนยันด้วย `getComputedStyle` ว่าไล่สีถูกต้อง
ตาม accent+ความสว่าง (ทดสอบ mint/flooks ทั้งสว่าง/มืด, gymes มืด)

**นอกขอบเขต** (ไม่ retint อัตโนมัติ, จะต้องแก้เพิ่มทีละไฟล์ถ้าต้องการในอนาคต):
- 4 หน้าที่ไม่มี `ome-tool-page`/`ome-app-page` เลย (404, bar-prep, cad3d, credits)
- กราฟ/ชาร์ตที่วาดด้วย JS ตรงๆ (sparkline ราคาทอง, lightweight-charts หุ้น, canvas ของ CAD)
  ใช้สี hex ฝังในโค้ด ไม่ได้อ่านจาก CSS variable เลย

**เพิ่มสีธีมใหม่ในอนาคต**: (1) เพิ่ม `[data-accent="X"]{--ome-*: ...}` (เวอร์ชันสว่าง) +
`[data-accent="X"][data-theme="dark"]{--ome-*: ...}` (เวอร์ชันมืด) 2 block ใน `theme.css`
(2) เพิ่มรายการใน `ACCENTS` array ใน `shell.js` (id/label/สีจุดสวอตช์) — ไม่ต้องแก้ไฟล์ HTML
หน้าใดเลย ไม่ต้องแก้ rule รีแมปพื้นผิว/`.hero` อีกต่อไป (ใช้ selector กลาง `[data-accent]`
ครอบคลุมสีใหม่โดยอัตโนมัติแล้ว)

**แผนต่อไป (ยังไม่ได้เริ่ม รอผู้ใช้ยืนยันรายละเอียด)**: แกนที่ 3 — "ประเภทธีม" (surface style)
เช่น กระจกฝ้า (glassmorphism), นูนนิ่ม (neumorphism), ดิบเท่ (neubrutalism) ฯลฯ ทำงานอิสระ
ร่วมกับทั้ง 2 แกนบนได้ (เลือกสี + ความสว่าง + สไตล์พื้นผิว พร้อมกัน) — ตัดตัวเลือก "เรืองแสงมืด"
ออกจากรายชื่อผู้สมัครแล้ว เพราะบังคับพื้นหลังมืดในตัว ขัดกับแกนความสว่างที่ต้องพลิกได้อิสระ

## ภาษา UI รวมศูนย์ (เฟส 5) — จุดควบคุมเดียวทั้งเว็บ

ก่อนหน้านี้มี 11 หน้าเครื่องมือที่ทำระบบ 2 ภาษา (ไทย/อังกฤษ) ไว้แยกกันเองแล้ว
(`cad.html`, `excel.html`, `word.html`, `doc-check.html`/`doc-check-file.html`,
`report-dashboard.html`, `music.html`, `sports.html`, `coding.html`, `cooking.html`,
`typing.html`) แต่ละหน้ามีปุ่ม "ไทย/EN" ของตัวเอง และบางกลุ่มก็เก็บค่าคนละ
`localStorage` key กัน (`word`/`excel`/`cad`/`doc-check` เดิมใช้ร่วมกันแล้วที่
`tanot:doclang`, ส่วน `music`/`sports`/`coding`/`cooking`/`typing`/`report-dashboard`
แยกคนละคีย์) ทำให้สลับภาษาที่หน้าหนึ่งแล้วไปเปิดอีกหน้าต้องกดสลับใหม่ทุกครั้ง

**สิ่งที่ทำ**: รวมทุกคีย์เป็น `'ome:lang'` เดียว (แก้แค่ค่าคงที่ 1 บรรทัดต่อไฟล์ ไม่แตะ
logic เดิม) + เพิ่มจุดควบคุมกลางใน `shell.js` (`window.OME_LANG.get()/.set()`) +
เพิ่มแถว "ภาษา" ในเมนูตั้งค่า (ถัดจาก "เลือกธีมเว็บ") กดแล้วกางตัวเลือก ไทย/English
เหมือนกลไกเลือกธีม — เลือกแล้วบันทึก `ome:lang` แล้วเรียก `window.omeApplyLang()`
ของหน้าปัจจุบัน (ถ้ามี) ให้รีเฟรชป้ายทันทีไม่ต้องรีโหลดหน้า

**แต่ละไฟล์เครื่องมือ** expose `window.omeApplyLang` เป็นฟังก์ชันที่ทำสิ่งเดียวกับที่
ปุ่มสลับภาษาเดิมของหน้านั้นทำ (บางหน้าแค่ `applyStaticI18n()`, บางหน้ามีรีเฟรชเพิ่ม
เช่น `renderLayersPanel()`/`renderTrackTabs()` ฯลฯ) — คัดลอกลำดับ call เดิมจาก
click handler มาทั้งหมด ไม่ตัดจุดไหนออก เพื่อให้พฤติกรรมเหมือนกดปุ่มเดิมทุกจุด
ปุ่มสลับภาษาเดิมของแต่ละหน้ายังอยู่และใช้งานได้ปกติ (เขียน/อ่านคีย์กลางเดียวกันแล้ว)

**ผลที่ได้**: ตั้งค่าภาษาที่จุดไหนก็ได้ (เมนูตั้งค่ากลาง หรือปุ่มท้องถิ่นของหน้าเครื่องมือ
เดิม) แล้วไปเปิดหน้าเครื่องมืออื่นในกลุ่ม 11 หน้านี้ จะเห็นเป็นภาษาที่ตั้งไว้ทันที
ไม่ต้องกดสลับซ้ำ — ยืนยันด้วย Playwright ทั้ง 11 หน้า

**นอกขอบเขต** (ยังไม่มีผล เพราะไม่เคยมี dictionary คำแปลมาก่อน): ~30 หน้าที่เหลือ
(invest*, budget, classroom-*, languages.html, legal.html ฯลฯ) — ตั้งค่าภาษาจากจุด
กลางจะไม่ทำให้หน้าพวกนี้เปลี่ยนภาษา จนกว่าจะทำ dictionary คำแปล + `data-i18n`
ให้แต่ละหน้าเพิ่มเอง (งานเนื้อหาแยกต่างหาก ไม่ใช่ส่วนของกลไกกลางนี้) รวมถึงเปลือกเว็บ
เอง (เมนู/ปุ่มของ `shell.js`) ก็ยังเป็นภาษาไทยล้วน ยังไม่มี dictionary เช่นกัน

**เพิ่มเครื่องมือใหม่เข้าระบบภาษากลางในอนาคต**: ให้ไฟล์นั้นอ่าน/เขียน `localStorage`
คีย์ `'ome:lang'` ตรงๆ (ค่า `'th'`/`'en'`) แล้ว expose `window.omeApplyLang = function
(){ /* รีเฟรชป้ายทั้งหมดของหน้านี้ */ }` — จุดกลางจะเรียกให้เองอัตโนมัติ ไม่ต้องแก้ `shell.js`

## ประเภทธีม (เฟส 7) — แกนที่ 3 อิสระ: พื้นผิว

หลังเฟส 6 แยกสี (`data-accent`) กับความสว่าง (`data-theme`) ออกจากกันได้แล้ว ผู้ใช้ขอแกนที่ 3
เพิ่ม: "ประเภทธีม" คุมลักษณะพื้นผิว (กระจกฝ้า/นูนนิ่ม/ดิบเท่ ฯลฯ) แยกอิสระจากทั้งสีและความสว่าง
— เลือกสีอะไร + สว่าง/มืดแบบไหนก็ได้ ประเภทธีมเดียวกันให้ "ลักษณะพื้นผิว" เดียวกันเสมอ (ตัวอย่าง
ที่ผู้ใช้อธิบาย: กระจกฝ้า+น้ำเงิน แล้วเปลี่ยนเป็นกระจกฝ้า+แดง ก็ยังเบลอเหมือนเดิม แค่คนละสี)

**8 ประเภทที่ทำ** (`id` / label / คำอธิบายสั้น):
- `flat` เรียบมาตรฐาน — ค่าเริ่มต้น ไม่มี override ใดๆ เท่ากับพฤติกรรมเดิมก่อนเฟสนี้ทุกอย่าง
- `glass` กระจกฝ้า (Glassmorphism) — การ์ด/ปุ่มโปร่งแสง + `backdrop-filter: blur()` พื้นหน้าเว็บ
  มีแสงสีธีมจางๆ ให้เห็นเอฟเฟกต์เบลอชัดขึ้น
- `neumorph` นูนนิ่ม (Neumorphism) — การ์ดกลืนสีกับพื้นหลัง ไม่มีขอบ ใช้เงาคู่ (สว่าง/เข้ม) แทน
  ความนูน สูตรเงาต่างกันระหว่างสว่าง/มืดเพราะทิศทาง "แสงตก" ต้องเป็นเงาสว่างกว่าเสมอ
- `neubrutal` ดิบเท่ (Neubrutalism) — ขอบหนาสีตัวอักษรหลัก + เงาทึบ offset ไม่เบลอ กดปุ่มแล้ว
  ขยับตามเงา (`:active`)
- `outline` เส้นขอบ (Outline) — ไม่มีเงาเลย ใช้แค่เส้นขอบบาง เรียบที่สุดในบรรดาทั้งหมด
- `clay` คลุกดิน (Claymorphism) — โค้งมนมาก ใช้เงา inset สว่าง/เข้มผสมเงาลอยปกติ ให้ดูนูนหนา
  แบบดินปั้น
- `mica` อะคริลิกฝ้า (Fluent Mica/Acrylic) — โปร่งแสงเบามาก เบลอน้อยกว่ากระจกฝ้าเยอะ (แนว Mica
  ของ Windows: "ทึบเกือบหมด แค่ให้รู้สึกถึงพื้นหลัง")
- `aurora` ม่านสีลอย (Aurora) — ขอบการ์ดเป็นเส้นไล่สี (gradient border) + แสงเรืองรอบการ์ด (glow
  shadow) สีตามธีม + พื้นหลังหน้ามีแสงจางๆ ลอยเป็นก้อน

(ตัด "เรืองแสงมืด"/Neon-Cyberpunk ออกจากรายชื่อผู้สมัครไปตั้งแต่ก่อนเริ่มทำ เพราะบังคับพื้นหลังมืด
ในตัว ขัดกับที่ต้องการให้สว่าง/มืดเป็นอิสระจากประเภทธีม)

**สถาปัตยกรรม**: attribute ใหม่ `data-style` บน `<html>` (คู่กับ `data-accent`/`data-theme`
เดิม) — `shell.js` มี `STYLES` array + `getStyleType()`/`setStyleGlobal()` แพทเทิร์นเดียวกับ
`ACCENTS`/`getAccent()`/`setAccentGlobal()` เป๊ะ ค่าเก็บที่ `localStorage['ome:style']`
default `'flat'` (คีย์ใหม่ทั้งหมด ไม่ต้อง migrate จากอะไรเดิม) เมนูตั้งค่าเพิ่มแถบย่อย
"ประเภทธีม" อยู่ในกลุ่มเดียวกับ "เลือกธีมเว็บ" (ตามที่ผู้ใช้ขอ "ในธีมเพิ่มแถบย่อยประเภทธีม")
กลไกกางรายการเดียวกับสวอตช์สี ต่างแค่ใช้ไอคอนอีโมจิแทนจุดสี (ไม่มีสีให้แสดง)

**วิธี override**: หน้าเครื่องมือส่วนใหญ่ (`body.ome-tool-page`) นิยาม `.card`/`.btn` ซ้ำกันทุก
ไฟล์ (40+ ไฟล์) ด้วยตัวแปรร่วมชุดเดียวกัน (`var(--card)`/`var(--sh)`/`var(--ome-card-r)` ฯลฯ)
— เลยรวมศูนย์ทั้งหมดไว้ที่ `theme.css` ที่เดียว ด้วย selector `[data-style="X"]
body.ome-tool-page .card{...}`/`.btn{...}` + `!important` ไม่ต้องแก้ `.card{}` ทีละไฟล์เลย
สีที่ใช้ในแต่ละสูตร (`glass`/`aurora` ฯลฯ) มาจาก `color-mix(in srgb, var(--ome-*) N%,
transparent)` เพื่อให้ทุกประเภทธีมทำงานร่วมกับสีธีม (`data-accent`) ทั้ง 9 สีได้อัตโนมัติ
โดยไม่ต้องเขียนสูตรแยกทีละสี

**ขอบเขต**: มีผลเฉพาะ `body.ome-tool-page` เท่านั้น — ไม่รวม `body.ome-app-page` (4 หน้า
React+Tailwind ที่แก้เรื่องสีไปแล้วในเฟส 6.1-6.3: classroom-business/engineering, languages,
legal) เพราะโครงสร้าง class ต่างกันมาก (Tailwind utility ตรงๆ ไม่มี `.card`/`.btn` ร่วมกัน)
จะทำเพิ่มทีหลังถ้าผู้ใช้ต้องการ

**ทดสอบ**: Playwright ยืนยัน (1) เมนูตั้งค่าแสดงครบ 8 ตัวเลือก กดแล้วอัปเดต `data-style`/
`localStorage`/สถานะติ๊กถูกต้อง (2) `getComputedStyle` ของ `.card`/`.btn` จริงในหน้า
invest-gold.html ตรงตามที่ตั้งใจครบทั้ง 8 ประเภท × สว่าง/มืด (background/border-radius/
box-shadow/backdrop-filter/border) — ยืนยันเป็นพิเศษว่า `neumorph` ใช้เงาคนละสูตรถูกต้องระหว่าง
สว่าง/มืดจริง (3) ถ่ายภาพหน้าจอทั้ง 16 แบบ (8 ประเภท × 2 ความสว่าง) ตรวจด้วยตา อ่านง่าย ไม่มี
จุดพัง ไม่มี error จากคอนโซล (4) เช็ค brace ของ `theme.css` สมดุล

**เฟส 7.1 — ขยายให้ครอบคลุม 4 หน้า React+Tailwind (`ome-app-page`)** ผู้ใช้ขอให้ทำเพิ่มหลังเห็นว่า
เฟส 7 มีผลแค่ `body.ome-tool-page` เท่านั้น หน้าพวกนี้ (classroom-business/engineering, languages,
legal) ไม่มี `.card`/`.btn` ร่วมกันแบบหน้าเครื่องมือ (ใช้ Tailwind utility ตรงๆ) — เลยใช้ `.bg-white`
เป็น surface หลักแทน (พิสูจน์แล้วด้วย grep ว่าใช้เฉพาะกับการ์ด/แผง/หัวข้อ/แถบข้างจริงๆ ไม่ชนกับ
badge/pill เล็กๆ ที่ใช้ `.bg-white/10`/`.bg-white/20` ซึ่งเป็นคนละคลาสกัน) ผูกกับโทเคนเดียวกับที่แก้
สีไปแล้วในเฟส 6.1 (`--ome-card`/`--ome-line`/`--ome-sh`) ตามสูตรเดียวกับเฟส 7 เป๊ะ ต่างแค่เปลี่ยน
`.card` → `.bg-white`

**ข้อจำกัดที่ตั้งใจ** (ต่างจากเฟส 7 ของหน้าเครื่องมือ): (1) ไม่แตะ `border-radius` เลยทุกประเภทธีม
เพราะ `.bg-white` ในหน้าพวกนี้ใช้ร่วมกับ `rounded-*` หลายขนาดปนกันในองค์ประกอบต่างชนิด เปลี่ยน
มุมทีเดียวเสี่ยงทำเลย์เอาต์บางจุดเพี้ยน — เอฟเฟกต์พื้นผิว (โปร่งแสง/เงา/ขอบ) ที่เหลือสื่อความรู้สึก
ของแต่ละประเภทธีมได้ชัดเจนอยู่แล้ว (2) ไม่แตะปุ่มหลัก (`.bg-primary`/`.bg-secondary`/`.bg-brand`)
เพราะปุ่มพวกนี้ในหน้า React ใช้สีแบรนด์ล้วนเป็นตัวชี้นำการกระทำหลัก (primary CTA) ต่างจาก `.btn`
ของหน้าเครื่องมือที่เป็นสีกลาง — การทำให้โปร่งแสง/จางสี/ลบสีออก (เช่น นูนนิ่มที่กลืนเป็นสีพื้น) จะลด
ความชัดเจนของปุ่มสำคัญลง เก็บเป็นสีทึบเหมือนเดิมเพื่อความปลอดภัยด้าน usability

**ทดสอบ**: จำลอง `.bg-white`/`.border`/`.border-slate-200`/`.shadow-sm` แบบเดียวกับที่ Tailwind
CDN จะ generate ให้จริง (เพราะ sandbox โหลด CDN จริงไม่ได้เหมือนเดิม) ยืนยันด้วย `getComputedStyle`
ว่าทุกประเภทธีม × สว่าง/มืด ได้ค่าสี/เงา/ความเบลอ/ขอบถูกต้องครบ และ border-radius ไม่ถูกแตะเลย
ตามที่ตั้งใจ — เช็ค brace ของ `theme.css` สมดุล
