# CAD Roadmap — บันทึกแผนงานสำหรับ `cad.html` / `cad.js` / `cad3d.js`

เอกสารนี้เป็น**บันทึกภายใน** เก็บสถานะปัจจุบันและ backlog ของเครื่องมือ CAD ไว้ให้ต่องานได้ทันทีในเซสชันถัดไป
โดยไม่ต้องไล่อ่านประวัติแชทซ้ำ — ไม่ได้ลิงก์จากหน้าเว็บ ไม่ต้องอยู่ใน `sw.js` PRECACHE (เป็นเอกสารสำหรับ
คนพัฒนา ไม่ใช่หน้าที่ผู้ใช้ต้องเปิดออฟไลน์)

อัปเดตล่าสุด: หลัง Stage 11b (commit `4fdc467`, `sw.js` CACHE = `ome-v409`)

---

## 1) สถานะปัจจุบัน (สรุปสั้นๆ ว่ามีอะไรแล้วบ้าง)

- **แท็บ "ร่างภาพ 2 มิติ"** (`cad.js`): วาดเส้น/พอลีไลน์/สี่เหลี่ยม/วงกลม/ส่วนโค้ง/สปไลน์, มิติ (เส้น/รัศมี/
  เส้นผ่านศูนย์กลาง/มุม), leader, hatch, block library, snap วัตถุ/กริด, constraint solver (planegcs,
  Stage 8), export PNG/SVG/DXF/PDF, import DXF
- **แท็บ "มุมมอง 3 มิติ"** (`cad3d.js`, OpenCascade.js B-Rep kernel): กล่อง/ทรงกระบอก/ทรงกลม, รวม/ตัด/
  หาส่วนร่วม, feature tree แก้ไข/ลบ/ปิดใช้งานย้อนหลังได้ (Stage 10b), ร่างบนระนาบ Top/Front/Right (10c)
  หรือคลิกเลือกหน้าจริงของชิ้นงาน (10d), **ร่างภาพตรงในวิวพอร์ต 3 มิติได้เลย** (Stage 11: สี่เหลี่ยม/วงกลม/
  เส้นหลายจุดปิดรูป) พร้อมปุ่มลัด "✏️ ร่างภาพ 3 มิติทันที" คลิกเดียวจากแท็บไหนก็ได้ + เห็นผลเป็นชิ้นงาน 3 มิติ
  ทันทีหลังวาดเสร็จโดยไม่ต้องกดปุ่มแยก (Stage 11b)
- **กล่องแจ้งเตือน/ยืนยันของตัวเอง** (สีขาวเสมอ ไม่สลับตามธีม) แทน `alert()`/`confirm()` ของเบราว์เซอร์

### ข้อจำกัดที่ตั้งใจยอมรับไว้แล้ว (documented ในคอมมิตของแต่ละสเตจ)
- ชิ้นงานหลักได้ทีละ 1 ชิ้นเท่านั้น (ไม่มี multi-body/assembly)
- เลื่อนตำแหน่งได้อย่างเดียว ยังไม่มีหมุน/fillet/chamfer
- "หน้าที่เลือกเอง" (picked face) เป็นการอ้างอิงครั้งเดียว (one-shot ไม่ associative) — แก้ขั้นตอนก่อนหน้า
  จนรูปทรงเปลี่ยน ตำแหน่งหน้าที่บันทึกไว้จะไม่ขยับตาม ต้องเลือกใหม่เอง
- รองรับ extrude/revolve เท่านั้น ยังไม่มี sweep/loft
- เส้นขอบปิดจาก 2 มิติ (ทั้งวาดในแท็บ 2D และร่างสดในวิว 3 มิติ) รองรับเฉพาะ สี่เหลี่ยม/วงกลม/พอลีไลน์ปิด
  ที่ปิดอยู่แล้วในตัว — เส้นตรงหลายเส้นต่อกันเป็นวงปิด/ส่วนโค้ง/สปไลน์ยังไม่รองรับ (ต้องใช้ graph-walk หา
  วงปิดที่ซับซ้อนกว่านี้)
- ร่างภาพสดในวิว 3 มิติ (Stage 11): วาดได้ทีละ 1 รูปต่อการกดปุ่ม 1 ครั้ง (จบรูปแล้วออกจากโหมดอัตโนมัติ),
  ไม่มี snap/dimension บังคับขนาดตอนวาด, ไม่รองรับส่วนโค้ง/สปไลน์, แก้จุดที่วางไปแล้วไม่ได้ (ต้องยกเลิก
  แล้ววาดใหม่ทั้งรูป)
- OCCT (opencascade.js) โหลดจาก CDN — สภาพแวดล้อมพัฒนา (sandbox) นี้บล็อก CDN เสมอ ทำให้ทุกฟีเจอร์ที่
  ต้องพึ่งการสร้างรูปทรงจริง (rebuildShape/mesh) **ไม่เคยทดสอบ end-to-end จริงในสภาพแวดล้อมพัฒนาได้เลย
  สักครั้ง** ทดสอบได้เต็มที่แค่ตรรกะคณิตศาสตร์ล้วนๆ (plane mapping, unit tests) และการเดินสาย UI ที่ไม่ต้อง
  พึ่ง mesh จริง — ควรทดสอบยืนยันจริงบน production หลัง deploy ทุกครั้งที่แก้ส่วนที่เกี่ยวกับ mesh/OCCT

---

## 2) ฟีดแบ็กผู้ใช้ที่ยังไม่ได้แก้ (จากบทสนทนาล่าสุด — ตัดสินใจไว้ว่าจะทำเมื่อได้รับแจ้งให้ทำต่อ)

เรียงตามที่ประเมินว่า **คุ้มสุด** (ใช้ความพยายามน้อย ผลกระทบต่อผู้ใช้เยอะ) ไปหาน้อย:

### 2.1 โชว์ขนาดจริงระหว่างลากวาด (Live dimension readout)
ตอนนี้ลากสี่เหลี่ยม/วงกลมในโหมดร่างภาพสด 3 มิติ (Stage 11) ต้อง "เดาด้วยตา" ล้วนๆ ไม่มีตัวเลขบอกกว้าง/สูง/
รัศมีระหว่างลาก ต้องปล่อยเมาส์แล้วไปดูผลลัพธ์ทีหลัง
- **แนวทาง**: เพิ่มป้ายข้อความลอย (เช่น DOM `<div>` ตำแหน่ง absolute ติดตามเคอร์เซอร์ หรือ `THREE.Sprite`/
  `CSS2DRenderer`) โชว์ "50×30 มม." / "R25 มม." อัปเดตทุกครั้งที่ `updateLiveSketchPreview()` ถูกเรียก —
  ข้อมูล (u,v) มีอยู่แล้วในมือ แค่คำนวณ w/d หรือ r แล้วโชว์ผล ไม่ต้องเพิ่ม state ใหม่
- ผลกระทบ: สูง (แก้ปัญหา "วาดแล้วไม่รู้ขนาดจริง" ตรงๆ) / ความพยายาม: ต่ำ

### 2.2 Snap ยึดตัวเลขกลม (grid snap) ในโหมดร่างภาพสด 3 มิติ
คลิกตรงไหนก็ได้ทศนิยมเยอะๆ (เช่น 47.3×28.9mm) ไม่ใช่ 50×30mm สวยๆ — แท็บ 2D มี snap toggle
(`#snapToggleBtn`) อยู่แล้ว แต่โหมดร่างภาพสดในวิว 3 มิติยังไม่ได้เอามาปรับใช้
- **แนวทาง**: ใน `liveSketchRayHit()` หลังได้ (u,v) จาก `unmapPlanePoint()` ปัดเศษเข้าใกล้ grid step ที่สุด
  (เช่น `Math.round(v/step)*step`) ก่อน push เข้า `liveSketchPts` — reuse ค่า grid step เดียวกับที่แท็บ 2D
  ใช้อยู่แล้วถ้าเป็นไปได้ (เช็คตัวแปร/ค่าคงที่ที่ `cad.js` ใช้)
- ผลกระทบ: สูง / ความพยายาม: ต่ำ

### 2.3 วาดต่อเนื่องหลายรูปในเซสชันเดียว
ตอนนี้วาดได้ทีละ 1 รูปต่อการกดปุ่ม "ร่างภาพ 3 มิติทันที" 1 ครั้ง (จบรูปแล้วออกจากโหมด+auto-commit ทันที
ตาม Stage 11b) — อยากวาดแผ่นแล้วเจาะรู 2-3 รู ต้องกดปุ่มใหม่ทุกรอบ
- **แนวทาง**: ต้องคิดให้รอบคอบกว่าข้อ 2.1/2.2 เพราะกระทบการออกแบบ auto-commit ของ Stage 11b โดยตรง —
  ตัวเลือกที่เป็นไปได้: (ก) หลัง auto-commit ถามว่า "วาดรูปถัดไปไหม" แทนที่จะออกจากโหมดเงียบๆ (ข) แยกปุ่ม
  "วาดต่อ" ออกจาก "จบการร่าง" อย่างชัดเจน ให้ผู้ใช้เลือกเอง — ต้องตัดสินใจร่วมกับผู้ใช้ก่อนว่าอยากได้พฤติกรรม
  แบบไหน (ไม่ควรเดาเอง เพราะกระทบ flow ที่เพิ่งปรับตาม feedback รอบก่อนโดยตรง)
- ผลกระทบ: กลาง-สูง (ขึ้นกับว่าผู้ใช้วาดชิ้นงานซับซ้อนบ่อยแค่ไหน) / ความพยายาม: กลาง

### 2.4 พอลีไลน์: Backspace ลบจุดล่าสุด
วางจุดผิดระหว่างวาดเส้นหลายจุด ตอนนี้ต้องกด "ยกเลิก" ทิ้งทั้งรูปแล้วเริ่มใหม่ ยังลบแค่จุดล่าสุดไม่ได้
- **แนวทาง**: เพิ่ม keydown listener (`Backspace`) ระหว่าง `liveSketchActive && liveSketchTool==='polyline'`
  ให้ `liveSketchPts.pop()` แล้ว `updateLiveSketchHint()`/`updateLiveSketchPreview()` ใหม่ — งานเล็ก จุดเดียว
- ผลกระทบ: กลาง / ความพยายาม: ต่ำมาก

### 2.5 Fillet / Chamfer (ปัดมุมโค้ง/ตัดมุมเอียง)
งานวิศวกรรมจริงแทบทุกชิ้นต้องใช้ แต่เป็นฟีเจอร์ใหญ่กว่าข้ออื่นมาก (ต้องเพิ่ม UI เลือกขอบ + เรียก
`BRepFilletAPI_MakeFillet`/`BRepFilletAPI_MakeChamfer` ของ OCCT + จัดการ edge-picking คล้าย Stage 10d
แต่ซับซ้อนกว่าเพราะเป็นการเลือก "เส้นขอบ" ไม่ใช่ "หน้า")
- ผลกระทบ: สูงมาก (ฟีเจอร์ที่ผู้ใช้งานจริงคาดหวังว่ามี) / ความพยายาม: สูง — ควรแยกเป็นสเตจใหญ่ของตัวเอง

---

## 3) แนวคิดที่พูดคุยกันแล้วแต่ยังไม่ได้ตัดสินใจทำ (จากการเทียบกับ SolidWorks จริง)

จากบทสนทนาเรื่อง "เข้าใจ SolidWorks ตรงกันไหม" — ช่องว่างเชิงสถาปัตยกรรมที่ใหญ่กว่า backlog ข้อ 2 ข้างบน
(ทำได้แต่ใช้ความพยายามสูงกว่ามาก เก็บไว้เป็นตัวเลือกระยะยาว ไม่ใช่ quick win):

- **Constraint-driven live sketch ในวิว 3 มิติ**: ตอนนี้ Stage 11 แค่คลิกวางจุดตามตำแหน่งจริง ไม่ผูก
  geometric constraint (ขนาน/ตั้งฉาก/coincident ฯลฯ) เหมือนที่แท็บ 2D มี planegcs อยู่แล้ว (Stage 8) — ถ้า
  จะทำให้ "เหมือน SolidWorks จริง" เต็มรูปแบบ ต้องเอา constraint solver มาผูกกับการร่างสดในวิว 3 มิติด้วย
  (งานใหญ่ ต้องคิดสถาปัตยกรรมใหม่ว่าจะแชร์ solver เดียวกันข้ามสองบริบทยังไง)
- **Associative feature history เต็มรูปแบบ**: โดยเฉพาะ "หน้าที่เลือกเอง" (2.x ด้านบนไม่ครอบคลุม) ต้องแก้
  topological-naming problem ซึ่งเป็นปัญหาที่ยากแม้แต่ใน CAD kernel มืออาชีพ
- **Multi-body / Assembly**: เก็บได้หลายชิ้นงานอิสระ + mate constraints ระหว่างชิ้นงาน — งานใหญ่มาก
- **Sweep / Loft**: เพิ่มอีก 2 วิธีขึ้นรูปนอกจาก extrude/revolve

---

## วิธีใช้เอกสารนี้

เซสชันถัดไปที่ผู้ใช้แจ้งว่า "พัฒนา CAD ต่อ" — เปิดไฟล์นี้ก่อน แล้วถามผู้ใช้ว่าจะเอาข้อไหนใน backlog
(ส่วนที่ 2) หรือแนวคิดระยะยาว (ส่วนที่ 3) มาถัดไป แทนที่จะเดาเอง — อัปเดตไฟล์นี้ทุกครั้งที่ทำเสร็จ 1 ข้อ
(ย้ายจาก "ยังไม่ได้แก้" เป็นบันทึกไว้ในส่วนที่ 1) หรือมีฟีดแบ็กใหม่เพิ่มเข้ามา
